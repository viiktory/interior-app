import { BlogsCards, SubscribeNews } from '@/components'

const Blogs = () => {
  return (
    <section id="blogs">
      <BlogsCards />
      <SubscribeNews />
    </section>
  )
}

export default Blogs
